import React from 'react';
// import { render, screen } from '@testing-library/react';
// import Modal from "./Modal";

describe('AddButton', () => {
    test('renders an Modal', () => {
        // const onOpen = true;
        // const top = '100px';
        // const left = '100px';
        // const children = 'I am a modal!';
        //
        // render(<Modal onOpen={onOpen} top={top} left={left} onClose={() => (console.log('onClose'))}>{children}</Modal>);
        //
        // const modalContent = screen.getByText(/I am a modal!/i);
        // expect(modalContent).toBeInTheDocument();
    });
});
