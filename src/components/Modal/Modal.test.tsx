import React from 'react';
// import {fireEvent, render, screen} from '@testing-library/react';
import Modal from "./Modal";

describe('Modal', () => {
    test('renders an Modal', () => {
        // const onOpen = true;
        // const top = '100px';
        // const left = '100px';
        // const children = 'I am a modal!';
        // const onClickFunction = jest.fn();
        //
        // render(<Modal onOpen={onOpen} top={top} left={left} onClose={onClickFunction}>{children}</Modal>);
        //
        // const modalContent = screen.getByText(/I am a modal!/i);
        // expect(modalContent).toBeInTheDocument();
        //
        //
        // const closeButton = screen.getByText(/Close/i);
        // fireEvent.click(closeButton);
        // expect(onClickFunction.mock.calls.length).toEqual(1);
    });
});
